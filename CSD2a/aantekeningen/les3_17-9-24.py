import pygame
import time

note_intervals = [1, 0.5, 0.5, 1, 0.5, 0.5]

time_stamps = []

BPM = 120

quarternote_dur = BPM / 60 / 4

time_durations = []
total_dur = 0

for dur in time_durations:
    time_stamps.append(total_dur)
    total_dur = total_dur + dur


t_now = time.time()
t_relative = t_now - time.time()

while True:
    t_now = 



# import time

# t = time.time()

# for i in range(10):
#     print(t)
#     t = time.time()
#     time.sleep(1)
